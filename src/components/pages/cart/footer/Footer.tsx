import { FC } from "react";

const Footer: FC = () => (
  <footer className="w-full flex justify-center items-center section-padding-tb">
    <button className="bg-red text-white py-2 px-6 capitalize" type="button">
      remove all items
    </button>
  </footer>
);

export default Footer;
